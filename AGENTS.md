# HighlightHub（开黑剪辑台）— 项目记忆

## 权威需求文档

完整产品需求、技术约束和验收标准见 [PROJECT_SPEC.md](./PROJECT_SPEC.md)。
任何开发工作开始前必须先读该文档。本文件只做导航，不重复其内容。

## 项目一句话定位

面向普通游戏玩家的录像事件定位、高光候选筛选与轻量剪辑平台。
技术栈：Java 21 + Spring Boot 3（业务权威）、Python FastAPI + FFmpeg/OpenCV/OCR（媒体执行）、Vue 3 + TS + Vite 前端。
架构为模块化单体，禁止一开始拆微服务。

## 执行纪律（来自任务书，必须遵守）

1. 按阶段推进：阶段 1 先打通不依赖算法的完整闭环（认证→分片上传→探测预览→手动事件→工程编辑→任务队列→渲染→下载），阶段 2 才做自动识别。
2. 不用假数据冒充已完成能力；没执行的测试不声称通过；合成测试视频必须标明"合成测试"。
3. 用户尚未指定第一款游戏：先做适配器接口 + OCR 链路，真实游戏适配标"待验证"，不得虚构 VERIFIED。
4. 每阶段完成后更新 `docs/implementation-status.md`（已完成 / 当前任务 / 尚未实现 / 阻塞 / 已执行测试 / 下一条操作），恢复会话先读它，不重建项目。
5. Spring Boot 是业务与任务状态权威；Python Worker 不直接写业务库，通过内部 HTTP 接口领取/续租/回报任务。
6. 不要询问命名、颜色、目录等低风险选择，按任务书自行决定并记录。

## 当前状态（2026-09-08）

- 项目尚未开始编码，目录为空。
- 下一步：阶段 0 —— 检查环境（Java/Python/Node/Docker/FFmpeg）、固定版本、建立目录结构与状态文档，随后立即进入阶段 1。
